<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="ObligatorioTT.Views.SponsorFormPage"
             Title="Patrocinador">
    <ScrollView>
        <VerticalStackLayout Padding="16" Spacing="10">
            <Image x:Name="imgLogo" HeightRequest="140" Aspect="AspectFit" />
            <Button Text="Elegir logo" Clicked="OnElegirLogo" />

            <Entry x:Name="txtNombre" Placeholder="Nombre *" TextChanged="OnCampoChanged" />
            <Entry x:Name="txtDireccion" Placeholder="Dirección *" TextChanged="OnCampoChanged" />

            <Button x:Name="btnGuardar" Text="Guardar" IsEnabled="False" Clicked="OnGuardar" />
        </VerticalStackLayout>
    </ScrollView>
</ContentPage>
